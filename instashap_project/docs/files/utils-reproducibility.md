# File Reference: `utils/reproducibility.py`

## Purpose

This file contains reproducibility and runtime helpers.

## Main Functions

- `set_global_seed(...)`
- `resolve_device(...)`
- `ensure_dir(...)`
- `write_json(...)`

## Why It Matters

Reproducibility is a central project goal. This file keeps seed-setting, device selection, and artifact persistence consistent across the codebase.

