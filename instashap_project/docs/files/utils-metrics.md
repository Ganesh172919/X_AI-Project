# File Reference: `utils/metrics.py`

## Purpose

This file defines reusable metric utilities and metric result containers.

## Main Objects

- `RegressionMetrics`
- `ClassificationMetrics`
- `regression_metrics(...)`
- `classification_metrics(...)`
- `explanation_error(...)`
- `benchmark_callable(...)`

## Why It Matters

It separates metric logic from experiment orchestration, keeping the pipeline easier to test and extend.

